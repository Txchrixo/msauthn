export enum Roles {
  SHOP_OWNER = 'shop_owner',
}

export enum OauthProvider {
  GOOGLE = 'google',
  FACEBOOK = 'facebook',
}

export enum TokenType {
  REFRESH = 'refresh',
  ACCESS = 'access',
  RESETPASSWORD = 'reset_password',
}
